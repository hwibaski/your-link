package com.yourink.repository.tag;

import com.yourink.domain.tag.TagLinkMap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TagLinkMapRepository extends JpaRepository<TagLinkMap, Long> {
}
